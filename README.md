🛒 E-Commerce Backend API
Современный RESTful API для интернет-магазина, построенный на стеке Java 25, Spring Boot 3.5 и PostgreSQL. Проект реализует полный цикл работы с покупателем: от регистрации и выбора товаров до оформления заказа с автоматическим пересчетом складских остатков.

🚀 Основной функционал
Управление пользователями: Полноценная регистрация и аутентификация (Spring Security) с хешированием паролей.

Каталог товаров: CRUD-операции над товарами, контроль остатков на складе (stockQuantity) и статусов (ACTIVE/INACTIVE).

Гибкая корзина: * Добавление товаров с проверкой наличия.

Автоматическое объединение позиций при повторном добавлении.

Механизмы очистки «битых» связей (Cleanup logic).

Система заказов (Checkout): * Транзакционное оформление заказа (Transaction Management).

Фиксация цены товара в момент покупки (price_at_purchase).

Автоматическое списание товаров со склада при подтверждении.

Глобальная обработка ошибок: Централизованный ExceptionHandler для возврата понятных JSON-ответов вместо системных ошибок.

🛠 Технологический стек
Backend: Java 25, Spring Boot 3.5.9.

Data: Spring Data JPA (Hibernate 6), PostgreSQL.

Migration: Flyway (версионное управление схемой БД).

Security: Spring Security (Basic Auth / Stateless).

Utilities: Lombok (сокращение шаблонного кода), Jackson (JSON-сериализация с поддержкой Hibernate Lazy Loading).

API Testing: Postman.

📋 Структура базы данных
Users — данные клиентов и роли.

Products — каталог товаров, цены и остатки.

Carts & CartItems — временное хранилище выбранных товаров.

Orders & OrderItems — история покупок с зафиксированными ценами.

Как запустить?
Убедись, что PostgreSQL запущен и создана база ecommerce_db.

Настрой доступы в src/main/resources/application.yml.

Запусти проект через EcommerceApplication.java — Flyway автоматически создаст все таблицы.

Используй эндпоинт POST /api/orders/{userId} для финального теста покупки.
